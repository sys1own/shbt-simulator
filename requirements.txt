# Optional runtime dependencies for SHBT simulator data export and visualization.
# The core simulator only needs the compiled shbt_simulator Rust extension.

matplotlib>=3.5.0
h5py>=3.7.0
pandas>=1.5.0
pyyaml>=6.0
jsonschema>=4.0.0
